# 서버 설정
PORT=3001
NODE_ENV=production
API_KEY=your-super-secret-api-key-here

# CORS 설정 (FiveM 서버 IP 허용)
ALLOWED_ORIGINS=http://localhost:30120,http://your-fivem-server-ip:30120

# 이메일 서비스 설정 (gmail, smtp, outlook 중 선택)
EMAIL_SERVICE=gmail

# Gmail 설정 (EMAIL_SERVICE=gmail인 경우)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password  # Gmail 앱 비밀번호 (2단계 인증 필요)
EMAIL_FROM=noreply@yourdomain.com

# SMTP 설정 (EMAIL_SERVICE=smtp인 경우)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# Outlook/Hotmail 설정 (EMAIL_SERVICE=outlook인 경우)
# EMAIL_USER와 EMAIL_PASS를 Outlook 계정으로 설정